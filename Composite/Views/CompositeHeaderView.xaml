<UserControl x:Class="Composite.Views.CompositeHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:Composite.Common.Properties"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" FontFamily="Verdana">
    <UserControl.Resources>
        <Style x:Key="ImageButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter>
                                <ContentPresenter.Content>
                                    <Image Height="{Binding Height, RelativeSource={RelativeSource AncestorType=Button}}" 
                                        Width="{Binding Width, RelativeSource={RelativeSource AncestorType=Button}}"
                                        Source="{Binding Content, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </ContentPresenter.Content>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="WhiteSmoke"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RepeatButtonTransparent" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="{TemplateBinding Background}" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <ControlTemplate x:Key="SliderThumbHorizontalDefault" TargetType="{x:Type Thumb}">
            <Grid HorizontalAlignment="Center" UseLayoutRounding="True" VerticalAlignment="Center">
                <Path x:Name="grip" Data="M 0,0 C0,0 11,0 11,0 11,0 11,18 11,18 11,18 0,18 0,18 0,18 0,0 0,0 z" Fill="WhiteSmoke" Stroke="Gray" 
                      SnapsToDevicePixels="True" Stretch="Fill" StrokeThickness="1" UseLayoutRounding="True" VerticalAlignment="Center"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter Property="Fill" TargetName="grip" Value="LightGray"/>
                    <Setter Property="Stroke" TargetName="grip" Value="Gray"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
        <ControlTemplate x:Key="SliderHorizontal" TargetType="{x:Type Slider}">
            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TickBar x:Name="TopTick" Fill="{TemplateBinding Foreground}" Height="4" Margin="0,0,0,2" Placement="Top" Grid.Row="0" Visibility="Collapsed"/>
                    <TickBar x:Name="BottomTick" Fill="{TemplateBinding Foreground}" Height="4" Margin="0,2,0,0" Placement="Bottom" Grid.Row="2" Visibility="Collapsed"/>
                    <Border x:Name="TrackBackground" Background="Black" BorderThickness="1" Height="4.0" Margin="5,0" Grid.Row="1" VerticalAlignment="center">
                        <Canvas Margin="-6,-1">
                            <Rectangle x:Name="PART_SelectionRange" Fill="Black" Height="4.0" Visibility="Hidden"/>
                        </Canvas>
                    </Border>
                    <Track x:Name="PART_Track" Grid.Row="1">
                        <Track.DecreaseRepeatButton>
                            <RepeatButton Command="{x:Static Slider.DecreaseLarge}" Style="{StaticResource RepeatButtonTransparent}"/>
                        </Track.DecreaseRepeatButton>
                        <Track.IncreaseRepeatButton>
                            <RepeatButton Command="{x:Static Slider.IncreaseLarge}" Style="{StaticResource RepeatButtonTransparent}"/>
                        </Track.IncreaseRepeatButton>
                        <Track.Thumb>
                            <Thumb x:Name="Thumb" Focusable="False" Height="18" OverridesDefaultStyle="True" Template="{StaticResource SliderThumbHorizontalDefault}" VerticalAlignment="Center" Width="11"/>
                        </Track.Thumb>
                    </Track>

                </Grid>
            </Border>
        </ControlTemplate>
        <Style x:Key="customSlider" TargetType="{x:Type Slider}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template" Value="{StaticResource SliderHorizontal}"/>
        </Style>
        
    </UserControl.Resources>


    <Grid Background="White" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.Effect>
            <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
        </Grid.Effect>

        <TextBlock Grid.Column="0" Text="{Binding Title}" FontSize="14" Margin="5"/>

        <Border Grid.Column="1" BorderThickness="1,0,0,0" BorderBrush="Gray" Margin="5,0,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="{Binding HeaderMediaPlayerViewModel.NameSong}" VerticalAlignment="Center" Margin="5,0,0,0" FontSize="11" MaxWidth="500"/>

                <Grid Grid.Column="1" Margin="10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="{Binding HeaderMediaPlayerViewModel.CurrentTime}" Margin="0,0,5,0" VerticalAlignment="Center"/>

                    <Slider Grid.Column="1" Minimum="0" Maximum="{Binding HeaderMediaPlayerViewModel.Duration}" Value="{Binding HeaderMediaPlayerViewModel.Position, Mode=OneWay}"
                            IsMoveToPointEnabled="True" VerticalAlignment="Center" Style="{StaticResource customSlider}"
                            local:SliderBehavior.StartSeekCommand="{Binding HeaderMediaPlayerViewModel.StartSeekingCommand}"
                            local:SliderBehavior.StopSeekCommand="{Binding HeaderMediaPlayerViewModel.StopSeekingCommand}"/>

                    <TextBlock Grid.Column="2" Text="{Binding HeaderMediaPlayerViewModel.TotalTime}" Margin="5,0,0,0" VerticalAlignment="Center"/>

                </Grid>

                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Center" >
                    <Button Content="/Common/Images/back.png" Style="{StaticResource ImageButton}" Height="16" Width="16" RenderOptions.BitmapScalingMode="Fant"
                            Command="{Binding HeaderMediaPlayerViewModel.BackCommand}"/>
                    <Button Content="/Common/Images/playStop.png" Style="{StaticResource ImageButton}" Height="26" Width="26" RenderOptions.BitmapScalingMode="Fant"
                            Command="{Binding HeaderMediaPlayerViewModel.PlayCommand}"/>
                    <Button Content="/Common/Images/next.png" Style="{StaticResource ImageButton}" Height="16" Width="16" RenderOptions.BitmapScalingMode="Fant"
                            Command="{Binding HeaderMediaPlayerViewModel.NextCommand}"/>
                </StackPanel>

                <Slider Grid.Column="3" Margin="5" Minimum="0" Maximum="1" Value="{Binding HeaderMediaPlayerViewModel.Volume}" Width="80" Style="{StaticResource customSlider}"/>

                <Button Grid.Column="4" Content="/Common/Images/settingMediaPlayer.png" Style="{StaticResource ImageButton}" Height="18" Width="18" Margin="0,0,5,0"
                        RenderOptions.BitmapScalingMode="Fant" Command="{Binding HeaderMediaPlayerViewModel.OpenViewSettingMediaPlayerCommand}"/>

            </Grid>
        </Border>
        
    </Grid>
</UserControl>