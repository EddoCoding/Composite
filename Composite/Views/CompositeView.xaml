<Window x:Class="Composite.Views.CompositeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Composite.Views"
        MouseDown="MoveWindow" Background="Transparent" WindowStyle="None"
        WindowStartupLocation="CenterScreen" RenderOptions.BitmapScalingMode="NearestNeighbor"
        Title="{Binding Header.Title}" Height="850" Width="1500" ResizeMode="CanResizeWithGrip" 
        AllowsTransparency="True" FontFamily="Trebuchet MS"
        TextOptions.TextFormattingMode="Display" Icon="/Common/Images/composite.png">

    <Border CornerRadius="8" BorderThickness="1" BorderBrush="LightGray" Background="WhiteSmoke">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <local:CompositeHeaderView Grid.Row="0" DataContext="{Binding Header}"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <local:CompositeMainView Grid.Column="0" DataContext="{Binding Main}"/>

                <local:CompositeMenuView Grid.Column="1" DataContext="{Binding Menu}"/>

            </Grid>

        </Grid>
    </Border>

</Window>