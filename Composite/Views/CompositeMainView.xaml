<UserControl x:Class="Composite.Views.CompositeMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Composite.Views" mc:Ignorable="d"
             xmlns:helper="clr-namespace:Composite.Common.Helpers"
             xmlns:dd="urn:gong-wpf-dragdrop"
             d:DesignHeight="450" d:DesignWidth="800" FontFamily="Trebuchet MS"
             KeyboardNavigation.TabNavigation="None">
    <UserControl.Resources>

        <helper:MyDragHandler x:Key="MyDragHandler"/>

        <Style x:Key="ButtonTab" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border>
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="Red"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CustomTabItem" TargetType="TabItem">
            <Setter Property="Height" Value="25"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="WhiteSmoke"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border BorderThickness="1" BorderBrush="{TemplateBinding BorderBrush}" CornerRadius="4" Margin="0" Background="{TemplateBinding Background}">
                            <Grid Margin="5,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding TitleTab}" VerticalAlignment="Center" MaxWidth="200" TextTrimming="CharacterEllipsis"/>
                                <Button Grid.Column="1" Content="X" Margin="5,0,0,0" VerticalAlignment="Center" FontSize="15"
                                        Style="{StaticResource ButtonTab}" Command="{Binding CloseTabCommand}"/>
                                
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="Gray"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="Gray"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="BorderBrush" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="MoveTo">
            <Border BorderThickness="1" BorderBrush="Black" CornerRadius="4">
                <TextBlock Text="Move to" Margin="5,3"/>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <TabControl Grid.Row="0" Background="WhiteSmoke" Margin="5,0,0,10" BorderThickness="0" ItemContainerStyle="{StaticResource CustomTabItem}"
            ItemsSource="{Binding TabService.Tabs}" SelectedItem="{Binding TabService.SelectedTab}"
            dd:DragDrop.IsDragSource="True" 
            dd:DragDrop.IsDropTarget="True" 
            dd:DragDrop.DragHandler="{Binding DragHandler}"
            dd:DragDrop.DropHandler="{Binding DragHandler}"
            dd:DragDrop.EffectMoveAdornerTemplate="{StaticResource MoveTo}">
        <TabControl.ContentTemplate>
            <DataTemplate>
                <ContentPresenter Content="{Binding ContentTab}"/>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>

</UserControl>