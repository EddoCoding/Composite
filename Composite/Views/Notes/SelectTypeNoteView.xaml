<Window x:Class="Composite.Views.Notes.SelectTypeNoteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Composite.Views"
        MouseDown="MoveWindow" Background="WhiteSmoke" WindowStyle="None" 
        WindowStartupLocation="CenterScreen" RenderOptions.BitmapScalingMode="NearestNeighbor"
        Title="{Binding Title}" MinHeight="360" MinWidth="650" MaxHeight="360" MaxWidth="650" ResizeMode="CanResizeWithGrip" 
        AllowsTransparency="True" BorderBrush="LightGray" BorderThickness="1" FontFamily="Trebuchet MS">
    <Window.Resources>
        <Style x:Key="defaultNoteButton" TargetType="Button">
            <Setter Property="Background" Value="WhiteSmoke"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Padding="10,10,5,0" VerticalAlignment="Top">
                            <StackPanel>

                                <TextBlock Text="Простая заметка" FontSize="17" HorizontalAlignment="Center"/>

                                <TextBlock Text="{Binding DescriptionDefaultNote}" FontSize="13" LineHeight="18" Margin="0,10,0,0" TextOptions.TextFormattingMode="Display"/>

                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="WhiteSmoke"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="hardNoteButton" TargetType="Button">
            <Setter Property="Background" Value="WhiteSmoke"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" Padding="10,10,5,0" VerticalAlignment="Top">
                            <StackPanel>

                                <TextBlock Text="Функциональная заметка" FontSize="17" HorizontalAlignment="Center"/>

                                <TextBlock Text="{Binding DescriptionHardNote}" FontSize="13" LineHeight="18" Margin="0,10,0,0" TextOptions.TextFormattingMode="Display"/>

                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="WhiteSmoke"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Style="{StaticResource defaultNoteButton}" Margin="20,50,10,0" HorizontalAlignment="Center"
                Command="{Binding OpenDefaultNoteCommand}"/>

        <Button Grid.Column="1" Style="{StaticResource hardNoteButton}" Margin="10,50,20,0" HorizontalAlignment="Center"
                Command="{Binding OpenHardNoteCommand}"/>

        <Button Grid.ColumnSpan="2" Content="Отмена" Style="{StaticResource DefaultButton}" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                FontSize="16" Margin="0,0,10,10" Command="{Binding CloseCommand}"/>

    </Grid>

</Window>