<UserControl x:Class="Composite.Views.Notes.AddHardNoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Composite.Views.Notes"
             xmlns:viewModel="clr-namespace:Composite.ViewModels.Notes.HardNote"
             xmlns:property="clr-namespace:Composite.Common.Properties"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" FontFamily="Verdana" TextOptions.TextFormattingMode="Ideal">
    <UserControl.Resources>
        
        <Style x:Key="MainHeader" TargetType="TextBox">
            <Setter Property="Text" Value="{Binding Header}"/>
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontFamily" Value="Trebuchet MS"/>
            <Setter Property="SelectionBrush" Value="LightGray"/>
            <Setter Property="CaretBrush" Value="Gray"/>
            <Setter Property="MaxWidth" Value="800"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                                      HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Text" TargetType="TextBox">
            <Setter Property="Text" Value="{Binding Text}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontFamily" Value="Trebuchet MS"/>
            <Setter Property="SelectionBrush" Value="LightGray"/>
            <Setter Property="CaretBrush" Value="Gray"/>
            <Setter Property="MaxWidth" Value="800"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate DataType="{x:Type viewModel:HeaderComposite}">
            <TextBox Style="{StaticResource MainHeader}" property:PlaceHolder.PlaceHolder="Headline" PreviewKeyDown="TextBox_PreviewKeyDown"/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:TextComposite}">
            <TextBox Style="{DynamicResource Text}" property:PlaceHolder.PlaceHolder="Enter text" PreviewKeyDown="TextBox_PreviewKeyDown"/>
        </DataTemplate>

    </UserControl.Resources>

    <Grid>
        <ListView x:Name="listComposite" ItemsSource="{Binding HardNoteVM.Composites}" Margin="0,30,0,0" BorderThickness="0" HorizontalContentAlignment="Stretch"
                  PreviewKeyDown="ListView_PreviewKeyDown">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Style.Setters>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style.Setters>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>

</UserControl>