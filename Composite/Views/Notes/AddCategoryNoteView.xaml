<Window x:Class="Composite.Views.Notes.AddCategoryNoteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Composite.Views.Notes"
        MouseDown="MoveWindow" Background="WhiteSmoke" WindowStyle="None" 
        WindowStartupLocation="CenterScreen" RenderOptions.BitmapScalingMode="NearestNeighbor"
        Title="{Binding Title}" Height="130" Width="300" ResizeMode="CanResizeWithGrip" 
        AllowsTransparency="True" BorderBrush="LightGray" BorderThickness="1" FontFamily="Trebuchet MS"
        TextOptions.TextFormattingMode="Display" KeyboardNavigation.TabNavigation="None">
    <Window.Resources>
        <Style x:Key="Button" TargetType="Button">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" BorderThickness="0" HorizontalAlignment="Right" Background="White" Margin="0,8,8,8">
                            <Border.Effect>
                                <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                            </Border.Effect>

                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6"/>

                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="WhiteSmoke"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Введите название категории" Margin="10,10,10,0" FontSize="14"/>

        <TextBox x:Name="textBox" Grid.Row="1" Text="{Binding CategoryNoteVM.NameCategory}" Style="{StaticResource CustomTextBox2}" VerticalContentAlignment="Center"
                 Height="35" FontSize="14" Margin="10,10,10,0"/>

        <Grid Grid.Row="2" Margin="0,8,3,10" HorizontalAlignment="Right">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Add" Style="{StaticResource Button}" FontSize="14" Command="{Binding AddCategoryCommand}"/>
            <Button Grid.Column="1" Content="Cancel" Style="{StaticResource Button}" FontSize="14" Command="{Binding CloseCommand}"/>

        </Grid>

    </Grid>

</Window>