<UserControl x:Class="Composite.Views.Notes.Notes.ChangeHardNoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Composite.Views.Notes"
             xmlns:viewModel="clr-namespace:Composite.ViewModels.Notes.HardNote"
             xmlns:property="clr-namespace:Composite.Common.Properties"
             xmlns:converter="clr-namespace:Composite.Common.Converters"
             xmlns:helper="clr-namespace:Composite.Common.Helpers"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:dd="urn:gong-wpf-dragdrop"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" FontFamily="Trebuchet MS" 
             TextOptions.TextFormattingMode="Ideal" KeyboardNavigation.TabNavigation="None">
    <UserControl.Resources>
        <converter:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converter:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converter:EditingAndHoverToVisibilityConverter x:Key="EditingAndHoverToVisibilityConverter"/>

        <helper:DeleteHandler x:Key="DeleteHandler"/>
        
        <Style x:Key="PasswordTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="SelectionBrush" Value="LightGray"/>
            <Setter Property="CaretBrush" Value="Gray"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="FontFamily" Value="Trebuchet MS"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Grid.Row="1" x:Name="border" Margin="0,0,0,5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0,0,0,1" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Padding="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="PART_ContentHost" Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="Gray"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ImageButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter>
                                <ContentPresenter.Content>
                                    <Image Height="{Binding Height, RelativeSource={RelativeSource AncestorType=Button}}" 
                                           Width="{Binding Width, RelativeSource={RelativeSource AncestorType=Button}}"
                                           Source="{Binding Content, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </ContentPresenter.Content>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{Binding Tag, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SnippetButton" TargetType="Button">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <Grid Margin="3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Height="18" Width="18" Source="{Binding Tag, RelativeSource={RelativeSource AncestorType=Button}}"
                               RenderOptions.BitmapScalingMode="Fant"/>
                                <ContentPresenter Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{Binding Path=(property:ButtonHelper.AdditionalText), RelativeSource={RelativeSource AncestorType=Button}}"
                                   VerticalAlignment="Center" Foreground="Gray"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="WhiteSmoke"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="RichTextBoxButton" TargetType="{x:Type Button}">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="CommandTarget" Value="{Binding ElementName=mainRTB}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5" Padding="2">
                            <ContentPresenter>
                                <ContentPresenter.Content>
                                    <Image Height="{Binding Height, RelativeSource={RelativeSource AncestorType=Button}}" 
                                   Width="{Binding Width, RelativeSource={RelativeSource AncestorType=Button}}"
                                   Source="{Binding Content, RelativeSource={RelativeSource AncestorType=Button}}"
                                   Margin="{Binding Tag, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </ContentPresenter.Content>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="WhiteSmoke"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ComboBoxItemCustom2" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Stretch" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="WhiteSmoke"/>
                                <Setter Property="BorderBrush" Value="LightGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ComboBoxItemCustom3" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <StackPanel Orientation="Horizontal">
                                <Rectangle Fill="{Binding}" Height="13" Width="13"/>
                                <TextBlock Text="{Binding}" Margin="10,0,0,0"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="WhiteSmoke"/>
                                <Setter Property="BorderBrush" Value="LightGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="UpdateNoteButton" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="Save" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleTextBox" TargetType="TextBox">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="Margin" Value="150,10,0,5"/>
            <Setter Property="FontFamily" Value="Trebuchet MS"/>
            <Setter Property="SelectionBrush" Value="LightGray"/>
            <Setter Property="CaretBrush" Value="Gray"/>
            <Setter Property="MaxWidth" Value="1400"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                      HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Text" TargetType="TextBox">
            <Setter Property="Text" Value="{Binding Text, UpdateSourceTrigger=PropertyChanged}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Trebuchet MS"/>
            <Setter Property="SelectionBrush" Value="LightGray"/>
            <Setter Property="CaretBrush" Value="Gray"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Padding" Value="0,0,0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                              HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TextQuote" TargetType="TextBox">
            <Setter Property="Text" Value="{Binding Text, UpdateSourceTrigger=PropertyChanged}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="SelectionBrush" Value="LightGray"/>
            <Setter Property="CaretBrush" Value="Gray"/>
            <Setter Property="MinWidth" Value="85"/>
            <Setter Property="Margin" Value="3,-10"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                              HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Line" TargetType="Line">
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Stretch" Value="Fill"/>
            <Setter Property="X1" Value="0"/>
            <Setter Property="X2" Value="1"/>
            <Setter Property="Height" Value="5"/>
            <Setter Property="Margin" Value="10,10,0,0"/>
            <Setter Property="StrokeStartLineCap" Value="Round"/>
            <Setter Property="StrokeEndLineCap" Value="Round"/>
        </Style>
        <Style x:Key="CheckBoxTask" TargetType="CheckBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Margin" Value="10,5,0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" x:Name="CheckBoxBorder" Width="20" Height="20" Background="White" BorderBrush="DarkGray" 
                                    BorderThickness="2" CornerRadius="3" Margin="0,0,5,0" Cursor="Hand" VerticalAlignment="Top">
                                <Path x:Name="CheckMark" Data="M 0 4 L 4 8 L 12 0" Stroke="DarkGray" StrokeThickness="2" Visibility="Collapsed" 
                                      Stretch="Uniform" Margin="2"/>
                            </Border>

                            <TextBox x:Name="textBoxTask" Grid.Column="1" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"
                                     property:PlaceHolder.PlaceHolder="Enter task"
                                     PreviewKeyDown="ListView_TextBox_PreviewKeyDown">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="FontSize" Value="16"/>
                                        <Setter Property="FontFamily" Value="Trebuchet MS"/>
                                        <Setter Property="SelectionBrush" Value="LightGray"/>
                                        <Setter Property="CaretBrush" Value="Gray"/>
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="TextBox">
                                                    <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                                                          HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>

                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="textBoxTask" Property="TextDecorations" Value="Strikethrough"/>
                                <Setter TargetName="textBoxTask" Property="Foreground" Value="Gray"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Marker" TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Trebuchet MS"/>
            <Setter Property="SelectionBrush" Value="LightGray"/>
            <Setter Property="CaretBrush" Value="Gray"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="10,5,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                    HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Numeric" TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Trebuchet MS"/>
            <Setter Property="SelectionBrush" Value="LightGray"/>
            <Setter Property="CaretBrush" Value="Gray"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="10,5,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Code" TargetType="TextBox">
            <Setter Property="Text" Value="{Binding Text, UpdateSourceTrigger=PropertyChanged}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Hack"/>
            <Setter Property="SelectionBrush" Value="LightGray"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="CaretBrush" Value="Gray"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="Background" Value="WhiteSmoke"/>
            <Setter Property="Margin" Value="10,5,0,5"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="10,5,10,10">
                            <Border.Effect>
                                <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                            </Border.Effect>
                            <StackPanel>

                                <TextBlock Text="codeBlock" Panel.ZIndex="1" Margin="5,0,0,10" FontSize="14"/>

                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"
                                              HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Margin="15,0"/>

                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ToolTip x:Key="ToolTipDragDrop" x:Shared="False" BorderBrush="Gray" Background="White">
            <StackPanel>
                <TextBlock>
            <Run Text="Click" FontWeight="Bold" Foreground="Gray" FontSize="12"/>
            <Run Text="to open menu" FontSize="11"/>
                </TextBlock>
                <TextBlock HorizontalAlignment="Center">
            <Run Text="Drag" FontWeight="Bold" Foreground="Gray" FontSize="12"/>
            <Run Text="to move" FontSize="11"/>
                </TextBlock>
            </StackPanel>
        </ToolTip>

        <DataTemplate DataType="{x:Type viewModel:TextCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" Margin="0,2,0,0" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <TextBox x:Name="textBox" Grid.Column="2" Style="{StaticResource Text}" Margin="10,2,0,0"
                         property:PlaceHolder.PlaceHolder="Enter text or '/' for snippets" PreviewKeyDown="ListView_TextBox_PreviewKeyDown"/>

                <Button Grid.Column="3" Content="X" Cursor="Hand" VerticalAlignment="Top" VerticalContentAlignment="Top" Margin="10,3,0,0" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="4" Margin="5,3,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

                <Popup IsOpen="{Binding IsOpenSnippetsPopup, Mode=TwoWay}" PlacementTarget="{Binding ElementName=textBox}" AllowsTransparency="True" 
                       StaysOpen="False" Placement="Bottom">
                    <Border Width="200" CornerRadius="8" Background="White" Margin="3">
                        <Border.Effect>
                            <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                        </Border.Effect>

                        <StackPanel Margin="5">
                            <Button Style="{StaticResource SnippetButton}" Content="Formatted text" Tag="/Common/Images/ft.png" property:ButtonHelper.AdditionalText="/ft"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/ft">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Header1" Tag="/Common/Images/header1.png" property:ButtonHelper.AdditionalText="/h1"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/h1">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Header2" Tag="/Common/Images/header2.png" property:ButtonHelper.AdditionalText="/h2"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/h2">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Header3" Tag="/Common/Images/header3.png" property:ButtonHelper.AdditionalText="/h3"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/h3">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Quote" Tag="/Common/Images/quote.png" property:ButtonHelper.AdditionalText="/quote"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/quote">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Line" Tag="/Common/Images/line.png" property:ButtonHelper.AdditionalText="/line"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/line">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Task" Tag="/Common/Images/task.png" property:ButtonHelper.AdditionalText="/task"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/task">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Image" Tag="/Common/Images/image.png" property:ButtonHelper.AdditionalText="/img"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/img">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Ref" Tag="/Common/Images/ref.png" property:ButtonHelper.AdditionalText="/ref"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/ref">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="RefList" Tag="/Common/Images/refList.png" property:ButtonHelper.AdditionalText="/refs"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/refs">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Marker" Tag="/Common/Images/marker.png" property:ButtonHelper.AdditionalText="/marker"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/marker">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Numeric" Tag="/Common/Images/numeric.png" property:ButtonHelper.AdditionalText="/num"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/num">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Code" Tag="/Common/Images/code.png" property:ButtonHelper.AdditionalText="/code"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/code">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                            <Button Style="{StaticResource SnippetButton}" Content="Doc" Tag="/Common/Images/doc.png" property:ButtonHelper.AdditionalText="/doc"
                                    Command="{Binding DataContext.HardNoteVM.CreateCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                <Button.CommandParameter>
                                    <helper:ParametersCompositeString Value="/doc">
                                        <helper:ParametersCompositeString.CompositeBaseVM>
                                            <Binding/>
                                        </helper:ParametersCompositeString.CompositeBaseVM>
                                    </helper:ParametersCompositeString>
                                </Button.CommandParameter>
                            </Button>
                        </StackPanel>

                    </Border>
                </Popup>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:HeaderCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,2,5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" Margin="0,4,0,0" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <TextBox Grid.Column="2" Style="{StaticResource Text}" FontWeight="{Binding FontWeight}" FontSize="{Binding FontSize}" Margin="10,0,0,0"
                         property:PlaceHolder.PlaceHolder="Enter header" PreviewKeyDown="ListView_TextBox_PreviewKeyDown"/>

                <Button Grid.Column="3" Content="X" Margin="10,5,0,0" Cursor="Hand" VerticalAlignment="Top" VerticalContentAlignment="Top"  FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="4" Margin="5,5,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6" 
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:QuoteCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <Grid Grid.Column="2" Margin="10,0,0,0" VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="“" FontSize="18"/>

                    <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource TextQuote}"
                             property:PlaceHolder.PlaceHolder="Enter quote" 
                             PreviewKeyDown="ListView_TextBox_PreviewKeyDown"/>

                    <TextBlock Grid.Row="2" Grid.Column="2" Text="„" FontSize="18"/>

                </Grid>

                <Button Grid.Column="3" Content="X" Margin="10,5,0,0" Cursor="Hand" VerticalAlignment="Top" VerticalContentAlignment="Top" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="4" Margin="5,4,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:LineCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <Grid Grid.Column="2" Background="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="7,-2,0,-10">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Opacity" Value="0"/>
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.3" />
                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility" BeginTime="0:0:0" Duration="0:0:0">
                                                        <DiscreteObjectKeyFrame KeyTime="0:0:0">
                                                            <DiscreteObjectKeyFrame.Value>
                                                                <Visibility>Visible</Visibility>
                                                            </DiscreteObjectKeyFrame.Value>
                                                        </DiscreteObjectKeyFrame>
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3" />
                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility" BeginTime="0:0:0.3" Duration="0:0:0">
                                                        <DiscreteObjectKeyFrame KeyTime="0:0:0">
                                                            <DiscreteObjectKeyFrame.Value>
                                                                <Visibility>Collapsed</Visibility>
                                                            </DiscreteObjectKeyFrame.Value>
                                                        </DiscreteObjectKeyFrame>
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <Label Content="lineSize" Margin="0,0,0,1" Foreground="Gray"/>
                        <ComboBox ItemsSource="{Binding Lines}" SelectedItem="{Binding SelectedLineSize}" HorizontalAlignment="Left" Cursor="Hand" Margin="0,0,0,1"
                          Style="{StaticResource ComboBoxCustom}" ItemContainerStyle="{StaticResource ComboBoxItemCustom}"/>

                        <Label Content="lineColor" Margin="5,0,0,1" Foreground="Gray"/>
                        <ComboBox ItemsSource="{Binding Colors}" SelectedItem="{Binding SelectedLineColor}" HorizontalAlignment="Left" Cursor="Hand" Margin="0,0,0,1"
                          Style="{StaticResource ComboBoxCustom}" ItemContainerStyle="{StaticResource ComboBoxItemCustom3}"/>

                    </StackPanel>

                    <Line Grid.Row="1" Style="{StaticResource Line}" StrokeThickness="{Binding SelectedLineSize}" Stroke="{Binding SelectedLineColor}"/>

                </Grid>

                <Button Grid.Column="3" Content="X" Cursor="Hand" VerticalAlignment="Top" Margin="10,2,0,0" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="4" Margin="5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:TaskCompositeVM}">
            <Grid x:Name="rootPanel" MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,5,5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" Margin="0,6,0,0" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <CheckBox Grid.Column="2" Style="{StaticResource CheckBoxTask}" IsChecked="{Binding IsCompleted}" VerticalAlignment="Top"/>

                <Button Grid.Column="3" Content="X" Cursor="Hand" VerticalAlignment="Top" Margin="10,6,0,0" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="4" Margin="5,6,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:ImageCompositeVM}">
            <Border MaxWidth="1400" BorderThickness="1" BorderBrush="Transparent" Margin="0,5">
                <Grid Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="18"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                             property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                    <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                            RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" ToolTip="{StaticResource ToolTipDragDrop}"
                            Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                            CommandParameter="{Binding}"/>

                    <Grid Grid.Column="2" HorizontalAlignment="{Binding HorizontalImage}" Margin="10,0,0,0" VerticalAlignment="Top">

                        <Image Source="{Binding ImageSource}" Height="{Binding DisplayHeight}" Width="{Binding DisplayWidth}"
                       Cursor="Hand" RenderOptions.BitmapScalingMode="Fant" UseLayoutRounding="True" SnapsToDevicePixels="True"/>

                        <Border CornerRadius="12" VerticalAlignment="Top" HorizontalAlignment="Right" BorderThickness="0.5" BorderBrush="Gray" Background="White" Margin="0,5,5,0">
                            <StackPanel Orientation="Horizontal" Cursor="Hand"
                                Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Button Style="{StaticResource ImageButton}" Content="/Common/Images/AlignmentLeft.png" Height="20" Width="20" RenderOptions.BitmapScalingMode="Fant"
                                Tag="WhiteSmoke" Margin="3,2" Command="{Binding ChangeHorizontalCommand}" CommandParameter="Left"/>
                                <Button Style="{StaticResource ImageButton}" Content="/Common/Images/AlignmentCenter.png" Height="20" Width="20" RenderOptions.BitmapScalingMode="Fant" 
                                Tag="WhiteSmoke" Margin="1,2" Command="{Binding ChangeHorizontalCommand}" CommandParameter="Center"/>
                                <Button Style="{StaticResource ImageButton}" Content="/Common/Images/AlignmentRight.png" Height="20" Width="20" RenderOptions.BitmapScalingMode="Fant"
                                Tag="WhiteSmoke" Margin="3,2" Command="{Binding ChangeHorizontalCommand}" CommandParameter="Right"/>
                                <Button Style="{StaticResource ImageButton}" Content="/Common/Images/openImage.png" Height="16" Width="16" RenderOptions.BitmapScalingMode="Fant"
                                Tag="WhiteSmoke" Margin="1,2,3,2" Command="{Binding DataContext.HardNoteVM.OpenFullImageCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Border>

                    </Grid>

                    <Button Grid.Column="3" Content="X" Margin="10,0,0,0" Cursor="Hand" VerticalAlignment="Top" FontSize="15"
                            Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                            CommandParameter="{Binding}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <ContentPresenter/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="Red"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Grid Grid.Column="4" Margin="5,0" VerticalAlignment="Top">

                        <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                                 Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                                 property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                            <i:Interaction.Behaviors>
                                <helper:FocusTextBoxBehavior/>
                            </i:Interaction.Behaviors>
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="LostFocus">
                                    <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBox>

                        <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                                VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                                Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                            <Button.Visibility>
                                <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                    <Binding Path="IsEditing"/>
                                    <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                                </MultiBinding>
                            </Button.Visibility>
                        </Button>

                    </Grid>

                </Grid>
            </Border>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:RefCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,7,5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" Margin="0,9,0,0" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <Button x:Name="RefButton" Grid.Column="2" Style="{StaticResource ImageButton}" Content="/Common/Images/ref.png" Tag="#E6E6E6" Height="17" Width="18" Margin="10,9,0,0"
                        Cursor="Hand" FontSize="14" RenderOptions.BitmapScalingMode="Fant" VerticalAlignment="Top" Command="{Binding OpenRefPopupCommand}"/>

                <Button Grid.Column="3" Margin="10,5" Cursor="Hand" HorizontalAlignment="Left" MinWidth="100" MaxWidth="300" VerticalAlignment="Top"
                        Command="{Binding CheckValuRefCommand}" CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border BorderThickness="1" CornerRadius="8" Height="25" Background="White">
                                            <Border.Effect>
                                                <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                                            </Border.Effect>
                                            <TextBlock Text="{Binding DataContext.Text, RelativeSource={RelativeSource TemplatedParent}}" Foreground="Gray"
                                               FontSize="14" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Padding="8,3" Background="Transparent"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="4" Content="X" Cursor="Hand" VerticalAlignment="Top" Margin="10,9,0,0" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="5" Margin="5,9,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

                <Popup IsOpen="{Binding IsRefPopup, Mode=TwoWay}" PlacementTarget="{Binding ElementName=RefButton}" AllowsTransparency="True" StaysOpen="False" 
                       Placement="Bottom" VerticalOffset="10">
                    <Border Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="8" Width="400">
                        <Border.Effect>
                            <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                        </Border.Effect>

                        <StackPanel Margin="10">

                            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" CornerRadius="8">
                                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Text}" Margin="3,5,3,0" FontSize="13" TextWrapping="NoWrap"
                                 property:PlaceHolder.PlaceHolder="URL or note" property:PlaceHolder.ShowAlways="True"/>
                            </Border>

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">

                                <Button x:Name="RefPopup" HorizontalAlignment="Left" Command="{Binding OpenNotesPopupCommand}" VerticalAlignment="Center" FontSize="14">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Cursor" Value="Hand"/>
                                            <Setter Property="Foreground" Value="Gray"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <TextBlock Text="select note" Foreground="{TemplateBinding Foreground}"/>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Foreground" Value="Black"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <TextBlock Text="{Binding Text}" VerticalAlignment="Center" Margin="5,0,0,0" MaxWidth="200" TextTrimming="CharacterEllipsis" FontSize="14"/>
                                <Button Command="{Binding ClearSelectedNoteCommand}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Cursor" Value="Hand"/>
                                            <Setter Property="Foreground" Value="Gray"/>
                                            <Setter Property="Margin" Value="5,0,0,0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <TextBlock Text="clear" Foreground="{TemplateBinding Foreground}" FontSize="14"/>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Foreground" Value="Black"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>

                            </StackPanel>

                        </StackPanel>

                    </Border>
                </Popup>

                <Popup IsOpen="{Binding IsNotesPopup, Mode=TwoWay}" PlacementTarget="{Binding ElementName=RefPopup}" AllowsTransparency="True" StaysOpen="False" Placement="Bottom"
                       VerticalOffset="20" HorizontalOffset="-10">
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="8" MaxHeight="250" Background="White">
                        <ListView ItemsSource="{Binding Notes}" BorderThickness="0" Margin="0,5" Background="White">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Button Command="{Binding DataContext.SelectNoteCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="White"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border CornerRadius="8" Margin="3" Background="{TemplateBinding Background}">
                                                                            <TextBlock Text="{Binding Title}" Margin="5" Width="175" FontSize="13" TextTrimming="CharacterEllipsis"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="WhiteSmoke"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </Border>
                </Popup>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:ReferenceCompositeVM}">
            <Grid Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="RefButton" Grid.Column="0" Style="{StaticResource ImageButton}" Content="/Common/Images/ref.png" Tag="#E6E6E6" Height="17" Width="17" Margin="5,9,5,0"
                        Cursor="Hand" FontSize="14" RenderOptions.BitmapScalingMode="Fant" VerticalAlignment="Top" Command="{Binding OpenRefPopupCommand}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <Button Grid.Column="1" Margin="0,5,0,5" Cursor="Hand" HorizontalAlignment="Left" MinWidth="100" MaxWidth="300" VerticalAlignment="Top"
                        Command="{Binding CheckValuRefCommand}" CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border BorderThickness="1" CornerRadius="8" Height="25" Background="White">
                                            <Border.Effect>
                                                <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                                            </Border.Effect>
                                            <TextBlock Text="{Binding DataContext.Text, RelativeSource={RelativeSource TemplatedParent}}" Foreground="Gray"
                               FontSize="14" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Padding="8,3" Background="Transparent"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="2" Content="X" Cursor="Hand" VerticalAlignment="Top" Margin="5,9,0,0" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.DeleteRefCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Popup IsOpen="{Binding IsRefPopup, Mode=TwoWay}" PlacementTarget="{Binding ElementName=RefButton}" AllowsTransparency="True" StaysOpen="False" 
                       Placement="Bottom" VerticalOffset="10">
                    <Border Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="8" Width="400">
                        <Border.Effect>
                            <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                        </Border.Effect>

                        <StackPanel Margin="10">

                            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" CornerRadius="8">
                                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Text}" Margin="3,5,3,0" FontSize="13" TextWrapping="NoWrap"
                         property:PlaceHolder.PlaceHolder="URL or note" property:PlaceHolder.ShowAlways="True"/>
                            </Border>

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">

                                <Button x:Name="RefPopup" HorizontalAlignment="Left" Command="{Binding OpenNotesPopupCommand}" VerticalAlignment="Center" FontSize="14">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Cursor" Value="Hand"/>
                                            <Setter Property="Foreground" Value="Gray"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <TextBlock Text="select note" Foreground="{TemplateBinding Foreground}"/>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Foreground" Value="Black"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <TextBlock Text="{Binding Text}" VerticalAlignment="Center" Margin="5,0,0,0" MaxWidth="200" TextTrimming="CharacterEllipsis" FontSize="14"/>
                                <Button Command="{Binding ClearSelectedNoteCommand}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Cursor" Value="Hand"/>
                                            <Setter Property="Foreground" Value="Gray"/>
                                            <Setter Property="Margin" Value="5,0,0,0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <TextBlock Text="clear" Foreground="{TemplateBinding Foreground}" FontSize="14"/>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Foreground" Value="Black"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>

                            </StackPanel>

                        </StackPanel>

                    </Border>
                </Popup>

                <Popup IsOpen="{Binding IsNotesPopup, Mode=TwoWay}" PlacementTarget="{Binding ElementName=RefPopup}" AllowsTransparency="True" StaysOpen="False" Placement="Bottom"
                       VerticalOffset="20" HorizontalOffset="-10">
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="8" MaxHeight="250" Background="White">
                        <ListView ItemsSource="{Binding Notes}" BorderThickness="0" Margin="0,5" Background="White">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Button Command="{Binding DataContext.SelectNoteCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="White"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border CornerRadius="8" Margin="3" Background="{TemplateBinding Background}">
                                                                            <TextBlock Text="{Binding Title}" Margin="5" Width="175" FontSize="13" TextTrimming="CharacterEllipsis"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="WhiteSmoke"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </Border>
                </Popup>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:RefListCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,9,5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" Margin="0,11,0,0" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <Button Grid.Column="2" Style="{StaticResource ImageButton}" Content="/Common/Images/add.png" Height="21" Width="21" VerticalAlignment="Top" 
                        Margin="8,9,8,0" Cursor="Hand" RenderOptions.BitmapScalingMode="Fant" Tag="#E6E6E6"
                        Command="{Binding AddRefCompositeCommand}"/>

                <ListView Grid.Column="3" ItemsSource="{Binding References}" BorderThickness="0" Background="Transparent" VerticalAlignment="Top"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>

                <Button Grid.Column="4" Content="X" Cursor="Hand" VerticalAlignment="Top" Margin="5,12,0,0" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="5" Margin="5,11,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:MarkerCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,5,5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" Margin="0,6,0,0" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <Grid Grid.Column="2" Margin="10,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="•" FontSize="16" Margin="27,5,0,0"/>

                    <TextBox Grid.Column="1" Style="{StaticResource Marker}" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                             property:PlaceHolder.PlaceHolder="Enter marker text" PreviewKeyDown="ListView_TextBox_PreviewKeyDown"/>

                </Grid>

                <Button Grid.Column="3" Content="X" Cursor="Hand" VerticalAlignment="Top" Margin="10,6,0,0" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="4" Margin="5,6,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:NumericCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent" Margin="0,-3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,5,5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" Margin="0,6,0,0" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <Grid Grid.Column="2" Margin="10,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="{Binding Number}" FontSize="16" Margin="0,5,0,0" Width="16"/>

                    <TextBox Grid.Column="1" Style="{StaticResource Numeric}" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                             property:PlaceHolder.PlaceHolder="Enter numeric text" PreviewKeyDown="ListView_TextBox_PreviewKeyDown"/>

                </Grid>

                <Button Grid.Column="3" Content="X" Cursor="Hand" VerticalAlignment="Top" Margin="10,6,0,0" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="4" Margin="5,6,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:CodeCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,2,5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" Margin="0,5,0,0" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <TextBox Grid.Column="2" Style="{StaticResource Code}" VerticalAlignment="Top" PreviewKeyDown="ListView_TextBox_PreviewKeyDown_ForCodeBlock"/>

                <Button Grid.Column="3" Content="X" Cursor="Hand" VerticalAlignment="Top" Margin="10,5,0,0" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="4" Margin="5,5,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                            Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                            property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:DocCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,7,5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" Margin="0,9,0,0" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <Button Grid.Column="2" Style="{StaticResource ImageButton}" Content="/Common/Images/doc.png" Tag="#E6E6E6" Height="20" Margin="10,8,0,0"
                        Cursor="Hand" FontSize="14" RenderOptions.BitmapScalingMode="Fant" VerticalAlignment="Top"
                        Command="{Binding SelectDocumentCommand}"/>

                <Button Grid.Column="3" Margin="10,5" Cursor="Hand" HorizontalAlignment="Left" MinWidth="100" MaxWidth="300" VerticalAlignment="Top"
                        Command="{Binding OpenDocumentCommand}" CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border BorderThickness="1" CornerRadius="8" Height="25" Background="White">
                                            <Border.Effect>
                                                <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                                            </Border.Effect>
                                            <TextBlock Text="{Binding DataContext.Text, RelativeSource={RelativeSource TemplatedParent}}" Foreground="Gray"
                                               FontSize="14" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Padding="8,3" Background="Transparent"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="4" Width="18" Height="18" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Left">
                    <Ellipse Stroke="Gray" StrokeThickness="2" Opacity="0.3"/>
                    <Ellipse Stroke="#2196F3" StrokeThickness="2" StrokeDashArray="12,8" RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" Angle="0"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="SpinnerRotate" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>

                <Button Grid.Column="5" Content="X" Cursor="Hand" VerticalAlignment="Top" Margin="10,7,0,0" FontSize="15" HorizontalAlignment="Right"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="6" Margin="5,7,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:FormattedTextCompositeVM}">
            <Grid MaxWidth="1400" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Style="{StaticResource Text}" Text="{Binding Tag}" Margin="5,0" Width="100" TextWrapping="NoWrap" Foreground="Gray"
                         property:PlaceHolder.PlaceHolder="Enter tag" MaxLength="10" VerticalAlignment="Top"/>

                <Button x:Name="DragButton" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/actions.png" Height="18" Width="18" VerticalAlignment="Top"
                        RenderOptions.BitmapScalingMode="Fant" SnapsToDevicePixels="True" Cursor="Hand" Tag="#E6E6E6" Margin="0,1,0,0" ToolTip="{StaticResource ToolTipDragDrop}"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.OpenPopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                        CommandParameter="{Binding}"/>

                <Grid Grid.Column="2" Background="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="7,-2,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Opacity" Value="0"/>
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.3" />
                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility" BeginTime="0:0:0" Duration="0:0:0">
                                                        <DiscreteObjectKeyFrame KeyTime="0:0:0">
                                                            <DiscreteObjectKeyFrame.Value>
                                                                <Visibility>Visible</Visibility>
                                                            </DiscreteObjectKeyFrame.Value>
                                                        </DiscreteObjectKeyFrame>
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.3" />
                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility" BeginTime="0:0:0.3" Duration="0:0:0">
                                                        <DiscreteObjectKeyFrame KeyTime="0:0:0">
                                                            <DiscreteObjectKeyFrame.Value>
                                                                <Visibility>Collapsed</Visibility>
                                                            </DiscreteObjectKeyFrame.Value>
                                                        </DiscreteObjectKeyFrame>
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <Label Content="brSize" Margin="0,0,0,1" Foreground="Gray"/>
                        <ComboBox ItemsSource="{Binding Borders}" SelectedItem="{Binding SelectedBrSize}" HorizontalAlignment="Left" Cursor="Hand" Margin="0,0,0,1"
                          Style="{StaticResource ComboBoxCustom}" ItemContainerStyle="{StaticResource ComboBoxItemCustom}"/>

                        <Label Content="brCorner" Margin="5,0,0,1" Foreground="Gray"/>
                        <ComboBox ItemsSource="{Binding Corners}" SelectedItem="{Binding SelectedBrCornerRadius}" HorizontalAlignment="Left" Cursor="Hand" Margin="0,0,0,1"
                          Style="{StaticResource ComboBoxCustom}" ItemContainerStyle="{StaticResource ComboBoxItemCustom}"/>

                        <Label Content="brColor" Margin="5,0,0,1" Foreground="Gray"/>
                        <ComboBox ItemsSource="{Binding Colors}" SelectedItem="{Binding SelectedBrColor}" HorizontalAlignment="Left" Cursor="Hand" Margin="0,0,0,1"
                          Style="{StaticResource ComboBoxCustom}" ItemContainerStyle="{StaticResource ComboBoxItemCustom3}"/>

                        <Label Content="bgColor" Margin="5,0,0,1" Foreground="Gray"/>
                        <ComboBox ItemsSource="{Binding Colors}" SelectedItem="{Binding SelectedBgColor}" HorizontalAlignment="Left" Cursor="Hand" Margin="0,0,0,1"
                          Style="{StaticResource ComboBoxCustom}" ItemContainerStyle="{StaticResource ComboBoxItemCustom3}"/>

                    </StackPanel>

                    <Border Grid.Row="1" Background="{Binding SelectedBgColor}" BorderThickness="{Binding SelectedBrSize}" BorderBrush="{Binding SelectedBrColor}" CornerRadius="{Binding SelectedBrCornerRadius}"
                            Margin="4,0,0,5" Padding="3" >
                        <RichTextBox Name="mainRTB" AcceptsTab="True" FontSize="16" FontFamily="Trebuchet MS" SelectionBrush="LightGray"
                             Background="{Binding BgColor}" CaretBrush="Gray" BorderThickness="0"
                             property:RichTextBoxHelper.BindToViewModel="True"
                             property:RichTextBoxHelper.EnableFormattingPopup="True"
                             property:RichTextBoxHelper.EnableTextFormatting="True"
                             PreviewKeyDown="ListView_TextBox_PreviewKeyDown">
                            <RichTextBox.Resources>
                                <Style TargetType="Paragraph">
                                    <Setter Property="LineHeight" Value="6"/>
                                </Style>
                            </RichTextBox.Resources>
                        </RichTextBox>
                    </Border>

                    <Popup Name="formattingPopup" StaysOpen="False" Placement="RelativePoint" AllowsTransparency="True" PopupAnimation="Fade" Height="33" VerticalOffset="100">
                        <Border Background="White" CornerRadius="8" Padding="3" BorderBrush="Gray" BorderThickness="1">
                            <StackPanel Orientation="Horizontal">

                                <!--Шрифты-->
                                <Grid>
                                    <ComboBox x:Name="fontComboBox" ItemsSource="{Binding Fonts}" Style="{StaticResource ComboBoxCustom}" Cursor="Hand"
                                      ItemContainerStyle="{StaticResource ComboBoxItemCustom2}" IsTextSearchEnabled="False">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <Button Command="{x:Static helper:FormattedTextCommands.ChangeFont}"
                                                CommandParameter="{Binding}" CommandTarget="{Binding ElementName=mainRTB}">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="White"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">

                                                                        <TextBlock Background="{TemplateBinding Background}" Text="{Binding}" Padding="3" Margin="-1"/>

                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="WhiteSmoke"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>

                                    <TextBlock Text="Fonts" IsHitTestVisible="False" VerticalAlignment="Center" Margin="5,0,0,0" Background="White"/>
                                </Grid>

                                <!--Размеры шрифта-->
                                <Grid>
                                    <ComboBox x:Name="sizeComboBox" ItemsSource="{Binding FontSizes}" Style="{StaticResource ComboBoxCustom}" Cursor="Hand"
                                      ItemContainerStyle="{StaticResource ComboBoxItemCustom2}" IsTextSearchEnabled="False">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <Button Command="{x:Static helper:FormattedTextCommands.ChangeSize}"
                                                CommandParameter="{Binding}" CommandTarget="{Binding ElementName=mainRTB}">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="White"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">

                                                                        <TextBlock Background="{TemplateBinding Background}" Text="{Binding}" Padding="3" Margin="-1"/>

                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="WhiteSmoke"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>

                                    <TextBlock Text="Sizes" IsHitTestVisible="False" VerticalAlignment="Center" Margin="5,0,0,0" Background="White"/>
                                </Grid>

                                <!--Цвет текста-->
                                <Grid Margin="0,0,5,0">
                                    <ComboBox x:Name="textColorComboBox" ItemsSource="{Binding Colors}" Style="{StaticResource ComboBoxCustom}" Cursor="Hand"
                                      ItemContainerStyle="{StaticResource ComboBoxItemCustom2}" IsTextSearchEnabled="False">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <Button Command="{x:Static helper:FormattedTextCommands.ChangeTextColor}"
                                                CommandParameter="{Binding}" CommandTarget="{Binding ElementName=mainRTB}">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="White"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Grid Background="{TemplateBinding Background}" Margin="-1">
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="Auto"/>
                                                                                <ColumnDefinition Width="*"/>
                                                                            </Grid.ColumnDefinitions>

                                                                            <Rectangle Grid.Column="0" Fill="{Binding}" Height="13" Width="13" Margin="5,5,0,5"/>
                                                                            <TextBlock Grid.Column="1" Text="{Binding}" Margin="5"/>

                                                                        </Grid>

                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="WhiteSmoke"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>

                                    <TextBlock Text="TextColors" IsHitTestVisible="False" VerticalAlignment="Center" Margin="5,0,0,0" Background="White"/>
                                </Grid>

                                <!--Цвет заднего фона текста-->
                                <Grid Margin="0,0,5,0">
                                    <ComboBox x:Name="bgTextColorComboBox" ItemsSource="{Binding Colors}" Style="{StaticResource ComboBoxCustom}" Cursor="Hand"
                                      ItemContainerStyle="{StaticResource ComboBoxItemCustom2}" IsTextSearchEnabled="False">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <Button Command="{x:Static helper:FormattedTextCommands.ChangeBgTextColor}"
                                                CommandParameter="{Binding}" CommandTarget="{Binding ElementName=mainRTB}">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="White"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Grid Background="{TemplateBinding Background}" Margin="-1">
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="Auto"/>
                                                                                <ColumnDefinition Width="*"/>
                                                                            </Grid.ColumnDefinitions>

                                                                            <Rectangle Grid.Column="0" Fill="{Binding}" Height="13" Width="13" Margin="5,5,0,5"/>
                                                                            <TextBlock Grid.Column="1" Text="{Binding}" Margin="5"/>

                                                                        </Grid>

                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                                <Setter Property="Background" Value="WhiteSmoke"/>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>

                                    <TextBlock Text="BgTextColors" IsHitTestVisible="False" VerticalAlignment="Center" Margin="5,0,0,0" Background="White"/>
                                </Grid>

                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/bold.png" Tag="4" Command="EditingCommands.ToggleBold"/>
                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/italic.png" Tag="4" Command="EditingCommands.ToggleItalic"/>
                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/underlining.png" Tag="4" Command="EditingCommands.ToggleUnderline"/>
                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/strikethrough.png" Tag="4" Command="{x:Static helper:FormattedTextCommands.ToggleStrikethrough}"/>
                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/overline.png" Tag="4" Command="{x:Static helper:FormattedTextCommands.ToggleOverline}"/>


                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/alignLeft.png" Tag="2" RenderOptions.BitmapScalingMode="HighQuality" Command="EditingCommands.AlignLeft"/>
                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/alignCenter.png" Tag="1" RenderOptions.BitmapScalingMode="HighQuality" Command="EditingCommands.AlignCenter"/>
                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/alignRight.png" Tag="2" RenderOptions.BitmapScalingMode="HighQuality" Command="EditingCommands.AlignRight"/>
                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/alignJustify.png" Tag="1" RenderOptions.BitmapScalingMode="HighQuality" Command="EditingCommands.AlignJustify"/>

                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/addIndentation.png" Tag="1" RenderOptions.BitmapScalingMode="HighQuality" Command="EditingCommands.IncreaseIndentation"/>
                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/removeIndentation.png" Tag="1" RenderOptions.BitmapScalingMode="HighQuality" Command="EditingCommands.DecreaseIndentation"/>

                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/marker.png" Tag="2" RenderOptions.BitmapScalingMode="Fant" Command="EditingCommands.ToggleBullets"/>
                                <Button Style="{StaticResource RichTextBoxButton}" Content="/Common/Images/numeric.png" Tag="1" RenderOptions.BitmapScalingMode="Fant" Command="EditingCommands.ToggleNumbering"/>

                            </StackPanel>
                        </Border>
                    </Popup>

                </Grid>

                <Button Grid.Column="3" Content="X" Cursor="Hand" VerticalAlignment="Top" VerticalContentAlignment="Top" Margin="10,2,0,0" FontSize="15"
                        Visibility="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Command="{Binding DataContext.HardNoteVM.DeleteCompositeCommand, RelativeSource={RelativeSource AncestorType=ListView}}" 
                        CommandParameter="{Binding}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <ContentPresenter/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Grid Grid.Column="4" Margin="5,1,5,0" VerticalAlignment="Top">

                    <TextBox Style="{StaticResource Text}" Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Foreground="Gray" FontSize="12"
                             Width="150" VerticalAlignment="Center" Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}"
                             property:PlaceHolder.PlaceHolder="Enter comment" property:PlaceHolder.ShowAlways="True">
                        <i:Interaction.Behaviors>
                            <helper:FocusTextBoxBehavior/>
                        </i:Interaction.Behaviors>
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="LostFocus">
                                <i:InvokeCommandAction Command="{Binding DataContext.HardNoteVM.StopEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </TextBox>

                    <Button Style="{StaticResource ImageButton}" Content="/Common/Images/comment.png" Height="18" Width="18" RenderOptions.BitmapScalingMode="Fant" 
                            VerticalAlignment="Top" Cursor="Hand" Tag="#E6E6E6"
                            Command="{Binding DataContext.HardNoteVM.StartEditingCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}">
                        <Button.Visibility>
                            <MultiBinding Converter="{StaticResource EditingAndHoverToVisibilityConverter}">
                                <Binding Path="IsEditing"/>
                                <Binding Path="IsMouseOver" RelativeSource="{RelativeSource AncestorType=ListViewItem}"/>
                            </MultiBinding>
                        </Button.Visibility>
                    </Button>

                </Grid>

            </Grid>

        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModel:CommandContextMenu}">
            <Button Style="{StaticResource ImageTextButton}" Content="{Binding NameCommand}" FontSize="14" Tag="{Binding PathImage}" Height="18" 
                    Margin="3" RenderOptions.BitmapScalingMode="Fant" TextOptions.TextFormattingMode="Display"
                    Command="{Binding ActionCommand}" Click="PositionPopup"/>
        </DataTemplate>
        <DataTemplate x:Key="MoveTo">
            <Border BorderThickness="1" BorderBrush="Black" CornerRadius="4">
                <TextBlock Text="Move to" Margin="5,3"/>
            </Border>
        </DataTemplate>

    </UserControl.Resources>
    <UserControl.InputBindings>
        <KeyBinding Gesture="CTRL+S" Command="{Binding CheckNoteCommand}"/>
        <KeyBinding Gesture="CTRL+W" Command="{Binding CloseTabCommand}"/>
    </UserControl.InputBindings>

    <Grid PreviewKeyDown="Grid_PreviewKeyDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="titleTextBox" Grid.Row="0" Text="{Binding HardNoteVM.Title, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TitleTextBox}" 
                 property:PlaceHolder.PlaceHolder="Headline" property:PlaceHolder.ShowAlways="True" PreviewKeyDown="TitleTextBox_PreviewKeyDown"/>

        <ListView Grid.Row="1" x:Name="listComposite" ItemsSource="{Binding HardNoteVM.Composites}" BorderThickness="0" HorizontalContentAlignment="Stretch"
                  Focusable="False" Cursor="IBeam" ScrollViewer.CanContentScroll="False" Padding="0,0,0,0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                  dd:DragDrop.IsDragSource="True" dd:DragDrop.IsDropTarget="True" 
                  dd:DragDrop.DragHandler="{Binding HardNoteVM.DragHandler}"
                  dd:DragDrop.DropHandler="{Binding HardNoteVM.DragHandler}"
                  dd:DragDrop.EffectMoveAdornerTemplate="{StaticResource MoveTo}"
                  PreviewKeyDown="ListView_PreviewKeyDown" MouseDown="listComposite_MouseDown" PreviewMouseWheel="listComposite_PreviewMouseWheel">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Style.Setters>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style.Setters>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Grid Grid.Row="2" Margin="-5,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Style="{StaticResource ComboBoxCustom}" 
                      ItemContainerStyle="{StaticResource ComboBoxItemCustom}" FontSize="12" DisplayMemberPath="NameCategory" Cursor="Hand"/>

            <Button Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/lock.png" HorizontalAlignment="Right" Margin="10,0,0,0" Height="18" Width="18"
                    RenderOptions.BitmapScalingMode="Fant" VerticalAlignment="Center" Command="{Binding ShowPasswordCommand}" Cursor="Hand"/>

            <TextBox Grid.Column="2" Width="120" Style="{StaticResource PasswordTextBox}" FontSize="14" Margin="10,0,0,0" MaxLength="13" Background="Transparent"
                     Text="{Binding HardNoteVM.Password}" HorizontalAlignment="Right" Visibility="{Binding PasswordVisible}" VerticalAlignment="Bottom"
                     property:PlaceHolder.PlaceHolder="Enter password" property:PlaceHolder.ShowAlways="True"/>

            <Button Grid.Column="3" VerticalAlignment="Bottom" HorizontalAlignment="Center" Cursor="Hand" Command="{Binding CheckNoteCommand}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="q" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Background="White" Width="100">
                                        <Border.Effect>
                                            <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
                                        </Border.Effect>
                                        <StackPanel Orientation="Horizontal" Margin="3">

                                            <TextBlock Grid.Column="0" Text="Save" Foreground="DarkSlateGray" FontSize="16" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="ctrl + s" Foreground="DarkGray" Margin="15,0,0,0" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Right"/>

                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

        </Grid>

        <Grid Grid.Row="3" Background="WhiteSmoke" Margin="0,2,0,0">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Message}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <TextBlock Text="{Binding Message}" Foreground="OrangeRed" Margin="5,0,0,0"/>

        </Grid>

        <Popup x:Name="ImagePopup" IsOpen="{Binding HardNoteVM.IsImagePopupOpen, Mode=TwoWay}" Placement="Center" AllowsTransparency="True" StaysOpen="False">
            <Border BorderBrush="Gray" BorderThickness="0.5">
                <Image Source="{Binding HardNoteVM.Image}" Stretch="Uniform" MaxHeight="1000" MaxWidth="1000" RenderOptions.BitmapScalingMode="Fant"/>
            </Border>
        </Popup>

        <Popup IsOpen="{Binding HardNoteVM.IsOpenPopup, Mode=TwoWay}" Placement="Mouse" AllowsTransparency="True" StaysOpen="False">
            <Border Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
                <StackPanel>
                    <ListView ItemsSource="{Binding HardNoteVM.ContextMenu}" Padding="0,0,0,3" BorderThickness="1" BorderBrush="Transparent">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <ContentPresenter Cursor="Hand"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                </StackPanel>
            </Border>
        </Popup>

        <Popup x:Name="SecondPopup" IsOpen="{Binding HardNoteVM.IsOpenPopupType, Mode=TwoWay}" AllowsTransparency="True" StaysOpen="False" Placement="Right" HorizontalOffset="10">
            <Border Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
                <ListView ItemsSource="{Binding HardNoteVM.ContextMenuTypes}" Padding="0,0,0,3" BorderThickness="1" BorderBrush="Transparent">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ContentPresenter Cursor="Hand"/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Border>
        </Popup>

    </Grid>

</UserControl>