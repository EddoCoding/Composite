<UserControl x:Class="Composite.Views.CompositeMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Composite.Views" mc:Ignorable="d"
             xmlns:converter="clr-namespace:Composite.Common.Converters"
             xmlns:property="clr-namespace:Composite.Common.Properties"
             d:DesignHeight="450" d:DesignWidth="800" FontFamily="Trebuchet MS"
             KeyboardNavigation.TabNavigation="None">
    <UserControl.Resources>
        <converter:NoteTypeToIconConverter x:Key="NoteTypeToIconConverter"/>
        <converter:TupleForCheckPasswordConverter x:Key="TupleForCheckPasswordConverter"/>

        <Style x:Key="ImageModule" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="7" Padding="3,5" Background="WhiteSmoke">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock x:Name="q" Grid.Column="0" Text="{Binding ToolTip, RelativeSource={RelativeSource AncestorType=Button}}" FontSize="13" VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Right" TextOptions.TextFormattingMode="Ideal"/>
                                <Image Grid.Column="1" Source="{Binding Content, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Margin="10,0,5,0" RenderOptions.BitmapScalingMode="Fant"/>

                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="q" Property="Foreground" Value="{Binding Tag, RelativeSource={RelativeSource AncestorType=Button}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TextBoxSearch" TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SelectionBrush" Value="LightGray"/>
            <Setter Property="CaretBrush" Value="Gray"/>
            <Setter Property="FontFamily" Value="Trebuchet MS"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>

                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="Black" SnapsToDevicePixels="True" BorderThickness="0.5" CornerRadius="10">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Padding="0" Margin="3,0"/>
                            </Border>

                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderThickness" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ImageButton" TargetType="Button">
            <Setter Property="Background" Value="WhiteSmoke"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter>
                                <ContentPresenter.Content>
                                    <Image Height="{Binding Height, RelativeSource={RelativeSource AncestorType=Button}}" 
                                           Width="{Binding Width, RelativeSource={RelativeSource AncestorType=Button}}"
                                           Source="{Binding Content, RelativeSource={RelativeSource AncestorType=Button}}"/>
                                </ContentPresenter.Content>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{Binding Path=Tag, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ImageTextButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" Background="Transparent" CornerRadius="5">
                            <ContentPresenter>
                                <ContentPresenter.Content>
                                    <StackPanel Orientation="Horizontal" Height="{Binding Height, RelativeSource={RelativeSource AncestorType=Button}}">

                                        <Image Width="{Binding Width, RelativeSource={RelativeSource AncestorType=Button}}"
                                               Source="{Binding Tag, RelativeSource={RelativeSource AncestorType=Button}}"
                                               Margin="3,1,0,1"/>

                                        <TextBlock Text="{Binding Content, RelativeSource={RelativeSource AncestorType=Button}}" Margin="10,1,5,1" VerticalAlignment="Center"
                                                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=Button}}"/>

                                    </StackPanel>
                                </ContentPresenter.Content>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E6E6E6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CategoryListViewItem" TargetType="ListViewItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Button Command="{Binding DataContext.SortByCategoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <TextBlock Grid.Column="0" Text="{Binding NameCategory}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                                   Margin="5" Width="140" TextTrimming="CharacterEllipsis"/>

                                                        <Button Name="button" Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/deleteCategory.png" Height="22" Width="22" 
                                                                Margin="0,0,5,0" Tag="LightGray" Background="WhiteSmoke"
                                                                Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"/>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#E6E6E6"/>
                                                        <Setter TargetName="button" Property="Background" Value="#E6E6E6"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="WhiteSmoke"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Buttons" TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="5"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E6E6E6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="addNoteButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Margin="0,2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Title}" Foreground="{TemplateBinding Foreground}" FontSize="16" Margin="5" TextOptions.TextFormattingMode="Ideal"/>

                                <Button Grid.Column="1" Content="/Common/Images/threeDots.png" Style="{StaticResource ImageButton}" Margin="0,0,20,0" Height="20" Width="20" 
                                        RenderOptions.BitmapScalingMode="Fant" Tag="LightGray" Background="{TemplateBinding Background}" HorizontalAlignment="Right"
                                        Command="{Binding DataContext.OpenClosePopupCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>

                                <Popup IsOpen="{Binding DataContext.IsPopupOpen, RelativeSource={RelativeSource AncestorType=ListView}}" 
                                       PlacementTarget="{Binding RelativeSource={RelativeSource AncestorType=Grid}}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
                                    <Border Background="WhiteSmoke" BorderBrush="DarkGray" BorderThickness="1" CornerRadius="5" Width="200">
                                        <StackPanel>

                                            <ContentControl Name="SortContentControl">
                                                <ContentControl.Style>
                                                    <Style TargetType="ContentControl">
                                                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                                        <Setter Property="FontSize" Value="14"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ContentControl">
                                                                    <Border Background="{TemplateBinding Background}" Margin="5,2,5,5" CornerRadius="5">
                                                                        <Grid>
                                                                            <TextBlock Text="Sort" FontSize="14" Margin="5,5,0,5"/>
                                                                            <TextBlock Text=">" FontSize="14" Margin="0,5,5,5" HorizontalAlignment="Right"/>
                                                                        </Grid>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#E6E6E6"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ContentControl.Style>
                                                <ContentControl.Triggers>
                                                    <EventTrigger RoutedEvent="MouseEnter">
                                                        <BeginStoryboard Name="ShowPopup">
                                                            <Storyboard>
                                                                <BooleanAnimationUsingKeyFrames Storyboard.TargetName="SortPopup" Storyboard.TargetProperty="IsOpen">
                                                                    <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="True"/>
                                                                </BooleanAnimationUsingKeyFrames>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </EventTrigger>
                                                    <EventTrigger RoutedEvent="MouseLeave">
                                                        <BeginStoryboard Name="HidePopupDelayed">
                                                            <Storyboard>
                                                                <BooleanAnimationUsingKeyFrames Storyboard.TargetName="SortPopup" Storyboard.TargetProperty="IsOpen">
                                                                    <DiscreteBooleanKeyFrame KeyTime="0:0:0.2" Value="False"/>
                                                                </BooleanAnimationUsingKeyFrames>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </EventTrigger>
                                                </ContentControl.Triggers>
                                            </ContentControl>
                                            <ContentControl Name="CategoriesContentControl">
                                                <ContentControl.Style>
                                                    <Style TargetType="ContentControl">
                                                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                                        <Setter Property="FontSize" Value="14"/>
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ContentControl">
                                                                    <Border Background="{TemplateBinding Background}" Margin="5,2,5,5" CornerRadius="5">
                                                                        <Grid>
                                                                            <TextBlock Text="Categories" FontSize="14" Margin="5,5,0,5"/>
                                                                            <TextBlock Text=">" FontSize="14" Margin="0,5,5,5" HorizontalAlignment="Right"/>
                                                                        </Grid>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#E6E6E6"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ContentControl.Style>
                                                <ContentControl.Triggers>
                                                    <EventTrigger RoutedEvent="MouseEnter">
                                                        <BeginStoryboard Name="ShowPopupCategories">
                                                            <Storyboard>
                                                                <BooleanAnimationUsingKeyFrames Storyboard.TargetName="CategoriesPopup" Storyboard.TargetProperty="IsOpen">
                                                                    <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="True"/>
                                                                </BooleanAnimationUsingKeyFrames>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </EventTrigger>
                                                    <EventTrigger RoutedEvent="MouseLeave">
                                                        <BeginStoryboard Name="CategoriesHidePopupDelayed">
                                                            <Storyboard>
                                                                <BooleanAnimationUsingKeyFrames Storyboard.TargetName="CategoriesPopup" Storyboard.TargetProperty="IsOpen">
                                                                    <DiscreteBooleanKeyFrame KeyTime="0:0:0.2" Value="False"/>
                                                                </BooleanAnimationUsingKeyFrames>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </EventTrigger>
                                                </ContentControl.Triggers>
                                            </ContentControl>

                                            <Popup Name="SortPopup" PlacementTarget="{Binding ElementName=SortContentControl}" Placement="Right" AllowsTransparency="True" HorizontalOffset="10">
                                                <Popup.Triggers>
                                                    <EventTrigger RoutedEvent="MouseEnter">
                                                        <StopStoryboard BeginStoryboardName="HidePopupDelayed"/>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <BooleanAnimationUsingKeyFrames Storyboard.TargetProperty="IsOpen">
                                                                    <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="True"/>
                                                                </BooleanAnimationUsingKeyFrames>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </EventTrigger>
                                                    <EventTrigger RoutedEvent="MouseLeave">
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <BooleanAnimationUsingKeyFrames Storyboard.TargetProperty="IsOpen">
                                                                    <DiscreteBooleanKeyFrame KeyTime="0:0:0.2" Value="False"/>
                                                                </BooleanAnimationUsingKeyFrames>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </EventTrigger>
                                                </Popup.Triggers>
                                                <Border Background="WhiteSmoke" BorderBrush="DarkGray" BorderThickness="1" CornerRadius="5" Width="180">
                                                    <StackPanel>
                                                        <Button Content="by Title" Style="{StaticResource Buttons}" Margin="5,5,5,2" 
                                                                Command="{Binding DataContext.SortByTitleCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                                        <Button Content="by DateCreate" Style="{StaticResource Buttons}" Margin="5,2,5,5" 
                                                                Command="{Binding DataContext.SortByDateCreateCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                                    </StackPanel>
                                                </Border>
                                            </Popup>
                                            <Popup Name="CategoriesPopup" PlacementTarget="{Binding ElementName=CategoriesContentControl}" Placement="Right" AllowsTransparency="True" HorizontalOffset="10">
                                                <Popup.Triggers>
                                                    <EventTrigger RoutedEvent="MouseEnter">
                                                        <StopStoryboard BeginStoryboardName="HidePopupDelayed"/>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <BooleanAnimationUsingKeyFrames Storyboard.TargetProperty="IsOpen">
                                                                    <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="True"/>
                                                                </BooleanAnimationUsingKeyFrames>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </EventTrigger>
                                                    <EventTrigger RoutedEvent="MouseLeave">
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <BooleanAnimationUsingKeyFrames Storyboard.TargetProperty="IsOpen">
                                                                    <DiscreteBooleanKeyFrame KeyTime="0:0:0.2" Value="False"/>
                                                                </BooleanAnimationUsingKeyFrames>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </EventTrigger>
                                                </Popup.Triggers>
                                                <Border Background="WhiteSmoke" BorderBrush="DarkGray" BorderThickness="1" CornerRadius="5" Width="200">

                                                    <StackPanel>
                                                        <Button Content="+ Add category" Style="{StaticResource Buttons}" Margin="5,5,5,2" 
                                                                Command="{Binding DataContext.OpenAddCategoryNoteViewCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                                        <Button Content="Show all" Style="{StaticResource Buttons}" Margin="5,2" 
                                                                Command="{Binding DataContext.ShowAllCategoriesCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                                        <ListView ItemsSource="{Binding DataContext.Categories, RelativeSource={RelativeSource AncestorType=ListView}}" BorderThickness="0" Margin="5,2,5,5" Background="Transparent"
                                                                  ItemContainerStyle="{StaticResource CategoryListViewItem}"/>
                                                    </StackPanel>

                                                </Border>
                                            </Popup>

                                        </StackPanel>
                                    </Border>
                                </Popup>

                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E6E6E6"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NoteButton" TargetType="Button">
            <Setter Property="Background" Value="WhiteSmoke"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Margin="0,2">
                            <Grid Width="234" HorizontalAlignment="Left">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="170"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Source="{Binding ItemType, Converter={StaticResource NoteTypeToIconConverter}}" Height="24" Width="24" RenderOptions.BitmapScalingMode="Fant"/>
                                <TextBlock Grid.Column="1" Text="{Binding Title}" FontSize="16" Margin="5" TextTrimming="CharacterEllipsis" Foreground="{TemplateBinding Foreground}"
                                           TextOptions.TextFormattingMode="Ideal"/>
                                <Button x:Name="button" Grid.Column="2" Style="{StaticResource ImageButton}" Content="/Common/Images/threeDots.png" Tag="#E6E6E6"
                                        Height="20" Width="20" RenderOptions.BitmapScalingMode="Fant" Visibility="Collapsed" Click="button_Click"/>

                                <Popup x:Name="NotePopup" PlacementTarget="{Binding RelativeSource={RelativeSource AncestorType=Button}}" 
                                       Placement="Right" StaysOpen="False" AllowsTransparency="True">
                                    <Border Background="WhiteSmoke" BorderBrush="DarkGray" BorderThickness="1" CornerRadius="5" Width="170">
                                        <StackPanel>

                                            <Button Style="{StaticResource ImageTextButton}" Content="Duplicate" Tag="/Common/Images/duplicate.png" Height="20"
                                                    FontSize="14" Margin="5,5,5,2" Click="PopupButton_Click"
                                                    Command="{Binding DataContext.DuplicateNoteCommand, RelativeSource={RelativeSource AncestorType=ListView}}" CommandParameter="{Binding}"/>

                                            <Button Style="{StaticResource ImageTextButton}" Content="Delete note" Tag="/Common/Images/deleteCategory.png" Height="20"
                                                    FontSize="14" Margin="5,2,5,5"
                                                    Command="{Binding DataContext.OpenPopupPasswordCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                                                <Button.CommandParameter>
                                                    <MultiBinding Converter="{StaticResource TupleForCheckPasswordConverter}">
                                                        <Binding/>
                                                        <Binding Source="Delete"/>
                                                    </MultiBinding>
                                                </Button.CommandParameter>
                                            </Button>

                                        </StackPanel>
                                    </Border>
                                </Popup>

                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter TargetName="button" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="NoteListViewItem" TargetType="ListViewItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Button x:Name="ItemButton" Style="{StaticResource NoteButton}"
                                Command="{Binding DataContext.OpenPopupPasswordCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                            <Button.CommandParameter>
                                <MultiBinding Converter="{StaticResource TupleForCheckPasswordConverter}">
                                    <Binding/>
                                    <Binding Source="Open"/>
                                </MultiBinding>
                            </Button.CommandParameter>
                        </Button>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding ItemType}" Value="AddNoteButton">
                                <Setter TargetName="ItemButton" Property="Style" Value="{StaticResource addNoteButton}"/>
                                <Setter TargetName="ItemButton" Property="Command" Value="{Binding DataContext.AddNoteCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ListView Grid.Row="1" ItemsSource="{Binding Notes}" BorderThickness="0" Margin="5,10,5,0" Background="WhiteSmoke"
                  ItemContainerStyle="{StaticResource NoteListViewItem}" ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>

        <Button Grid.Row="2" Content="/Common/Images/scale.png" Height="30" Margin="5,0,5,5" VerticalAlignment="Bottom" 
                Style="{StaticResource ImageModule}" ToolTip="Scale" Command="{Binding ScaleCommand}" Tag="Orange"/>

        <Button Grid.Row="3" Content="/Common/Images/collapse.png" Height="30" Margin="5,0,5,5" VerticalAlignment="Bottom" 
                Style="{StaticResource ImageModule}" ToolTip="Collapse" Command="{Binding CollapseCommand}" Tag="Tomato"/>

        <Button Grid.Row="4" Content="/Common/Images/close.png" Height="30" Margin="5,0,5,5" VerticalAlignment="Bottom"
                Style="{StaticResource ImageModule}" ToolTip="Exit" Command="{Binding CloseCommand}" Tag="Red"/>

        <Popup IsOpen="{Binding IsPasswordPopupOpen}" AllowsTransparency="True" Placement="Center" StaysOpen="False">
            <Border CornerRadius="8" Background="WhiteSmoke" BorderBrush="Gray" BorderThickness="1">
                <Grid Margin="5">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Password" Margin="5,5,0,0"/>
                    <Grid Grid.Row="1" Width="200">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" Style="{StaticResource CustomTextBoxSearch}" Text="{Binding Password}" Margin="5,0,5,3" MaxLength="13"
                                 Background="WhiteSmoke" property:PlaceHolder.PlaceHolder="Enter password" property:PlaceHolder.ShowAlways="True"/>
                        <Button Grid.Column="1" Style="{StaticResource ImageButton}" Content="/Common/Images/input.png" Height="20" Width="20" Margin="5,0,3,0"
                                Tag="WhiteSmoke" Cursor="Hand" RenderOptions.BitmapScalingMode="Fant"
                                Command="{Binding CheckPasswordCommand}"/>

                    </Grid>
                </Grid>
            </Border>
        </Popup>

    </Grid>


</UserControl>