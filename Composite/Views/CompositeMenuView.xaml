<UserControl x:Class="Composite.Views.CompositeMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Composite.Views" mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" FontFamily="Verdana">
    <UserControl.Resources>
        <Style x:Key="ImageModuleButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="7" Padding="3,5">
                            <Grid>
                                <Image Source="{Binding Content, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E6E6E6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ImageModuleCollapseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="7" Padding="3,5">
                            <Grid>
                                <Image Source="{Binding Content, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Yellow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ImageModuleCloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="7" Padding="3,5">
                            <Grid>
                                <Image Source="{Binding Content, RelativeSource={RelativeSource AncestorType=Button}}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="OrangeRed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="White" Margin="10,0,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.Effect>
            <DropShadowEffect Opacity="0.3" ShadowDepth="1"/>
        </Grid.Effect>

        <Button Grid.Row="0" Content="/Common/Images/notes.png" Height="35" Width="35" Margin="3" Style="{StaticResource ImageModuleButton}" ToolTip="Заметки"
                Command="{Binding OpenNotesCommand}"/>
        
        <Button Grid.Row="1" Content="/Common/Images/tasks.png" Height="35" Width="35" Margin="3" Style="{StaticResource ImageModuleButton}" ToolTip="Задачи"
                Command="{Binding OpenTasksCommand}"/>
        
        <Button Grid.Row="2" Content="/Common/Images/projects.png" Height="35" Width="35" Margin="3" Style="{StaticResource ImageModuleButton}" ToolTip="Проекты"/>

        <Button Grid.Row="3" Content="/Common/Images/collapse.png" Height="33" Width="33" Margin="3" VerticalAlignment="Bottom" RenderOptions.BitmapScalingMode="Fant"
                Style="{StaticResource ImageModuleCollapseButton}" ToolTip="Свернуть" Command="{Binding CollapseCommand}"/>
        
        <Button Grid.Row="4" Content="/Common/Images/close.png" Height="33" Width="33" Margin="3" VerticalAlignment="Bottom" RenderOptions.BitmapScalingMode="Fant"
                Style="{StaticResource ImageModuleCloseButton}" ToolTip="Выход" Command="{Binding CloseCommand}"/>

    </Grid>

</UserControl>